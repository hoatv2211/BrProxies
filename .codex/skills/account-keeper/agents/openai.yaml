interface:
  display_name: "Account Keeper"
  short_description: "Xử lý ChatGPT 2FA và Account Keeper an toàn"
  brand_color: "#2563EB"
  default_prompt: "Dùng $account-keeper để debug hoặc chạy flow ChatGPT 2FA trong cùng phiên đăng nhập, xác minh secret mới và chỉ ghi kết quả sau khi provider chấp nhận mã."

policy:
  allow_implicit_invocation: true
